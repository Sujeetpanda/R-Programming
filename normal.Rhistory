minVal <- 0
maxVal <- 100
mn <- (maxVal - minVal)/2
# Generate numbers (mostly) from min to max
x <- rnorm(10000, mean = mn, sd = mn/3)
pop_sd <- sd(x)*sqrt((length(x)-1)/(length(x)))
pop_mean <- mean(height)
pop_mean <- mean(x)
z <- (72 - pop_mean) / pop_sd
p_yellow1 <- pnorm(72, pop_mean, pop_sd)
p_yellow2 <- pnorm(z)
p_yellow1
p_yellow2
//To find the confidence level
